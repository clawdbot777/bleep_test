flask>=3.0.0
werkzeug>=3.0.0
python-magic>=0.4.27
requests>=2.31.0
watchdog>=4.0.0
gunicorn>=21.2.0

# torch, torchaudio, and nemo_toolkit are managed explicitly in Dockerfile
# to ensure CUDA builds always win. Do NOT add them here.

# Optional: WhisperX fallback backend
# pip install whisperx>=3.1.1
