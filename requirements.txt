flask>=3.0.0
werkzeug>=3.0.0
python-magic>=0.4.27
requests>=2.31.0
watchdog>=4.0.0
gunicorn>=21.2.0
torch>=2.2.0

# Parakeet TDT — primary transcription backend (installed via Dockerfile)
nemo_toolkit[asr]>=2.2.0

# Optional: WhisperX fallback backend
# pip install whisperx>=3.1.1
